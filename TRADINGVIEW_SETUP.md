# TradingView 대체 경로 — 배포 전 테스트본

서버 로컬 테스트 90개 통과. 실서버 배포, Pine 컴파일, 실제 Telegram 수신은 아직 미검증.

## 현재 가능한 범위
`POST /webhook/tradingview`는 JSON의 secret을 필수 검증한다. 기술 점수 70 이상, 상대 거래량 1.5 이상, 방향별 봉 시가 대비 움직임 0.5 ATR 이상을 시험용 모멘텀 조건으로 사용한다. 횡보, whipsaw, EMA9 이격 2 ATR 이상은 차단한다. 같은 종목 20분, 전체 5분당 3개 제한이다. 실측 성과로 최적화한 기준은 아니다.

종합 점수는 null이며 80점 진입 신호를 생성하지 않는다. 뉴스·촉매 평가와 최신 호가가 연결되지 않았기 때문이다. 호가 없는 모멘텀 정보라는 점을 메시지에 표시한다. 기존 `/webhook`은 별도 경로이며 새 필터를 적용하지 않는다.

## 운영 연결에 필요한 것
1. 이 서버 패키지를 실제 Vercel 프로젝트에 배포한다. PostgreSQL 저장소와 기존 Telegram 환경 변수가 필요하다.
2. `TRADINGVIEW_WEBHOOK_SECRET`에 영문·숫자로 된 비밀값을 설정하고 Pine에도 동일하게 입력한다. 공개 문서에 비밀값을 기재하지 않는다.
3. 관리자 인증으로 `/tradingview/session`에 확인된 당일 거래소 캘린더(date/is_open/open/close)를 등록한다. 자동 캘린더 공급은 아직 없다. 캘린더가 없으면 알림은 보류된다.
4. 새 Pine `Market_Forge_V13_27_TV_Information_TEST.pine`을 TradingView에서 컴파일하고, 시험 날짜를 확인한 뒤 SPY 표준 1분 차트에 적용한다. 7개 bank별 알림을 각각 생성한다. URL은 배포 호스트의 `/webhook/tradingview`다.
5. 기본 `TV_INFORMATION_MODE=shadow`에서 실제 수신 기록을 확인한다. 이 상태에서는 전송하지 않는다.
6. `TV_INFORMATION_MODE=live` 전환 후 인증된 `/tradingview/deliver`를 별도 작업자가 호출해야 대기열을 Telegram으로 보낸다. 해당 작업자 배포·예약은 아직 없다. HTTP 송신 성공과 사용자 수신을 따로 확인한다.

이번 Pine은 기존 확정봉 엔진에 관측값을 추가한 테스트본이다. 봉마감 전 진입 엔진을 구현한 것이 아니다. 70종목 점수 동시 순위가 아니라 도착 순서로 전체 알림 한도를 사용한다. 실시간 조기 모멘텀 관측 전송은 후속 작업이다. 자동 뉴스 평가, 섹터 비교도 연결 전이다.

배포 프로젝트 주소가 필요하다. 주소 확인 전에는 이 파일을 적용 완료나 실제 알림 작동으로 해석하지 않는다.
